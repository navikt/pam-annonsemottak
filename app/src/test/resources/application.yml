server:
  port: 9016
spring:
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
    continue-on-error: true
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: none
  profiles:
    active: test
management:
  metrics:
    export:
      prometheus:
        enabled: false
scheduler:
  enabled: false
dexi:
  url: http://localhost.dexi
  api:
    username: dexi_username
    password: dexi_password
amedia:
  url: http://localhost:7010/api/pipek/v2/classified/search
proxy:
  url: http://155.55.60.117:8088
solr:
  url: http://localhost.solr
  core: 'maincore'

#TODO: Remove after transition from PAM-STILLING is complete
stillingfeed:
  annonsemottak:
    url: https://pamstilling-t1.oera.no/api/stillinger/feed/
  pagesize: 10
  poolsize: 10
  duration: 10
  scheduler:
    enabled: true